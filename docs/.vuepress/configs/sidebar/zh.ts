import type { SidebarConfig } from "@vuepress/theme-default";

export const zh: SidebarConfig = {
    "/base/": [
        {
            text: "Smart Picture",
            children: [
                "/base/index.md",
            ],
        },
    ],
};
